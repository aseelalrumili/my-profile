# Backend API URL
# Set this to your deployed backend URL (e.g., https://your-api.onrender.com/api)
# During local development, the Vite proxy handles /api → localhost:5200
VITE_API_URL=https://your-backend-domain.com/api
